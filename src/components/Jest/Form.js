import React from "react";

const Form = () => {
    return (
        <div id="in-form">
            <input type="text" id="input-in-form" />
            <button disabled>Submit</button>
        </div>
    );
};

export default Form;
